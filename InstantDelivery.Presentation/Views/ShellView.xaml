<controls:MetroWindow x:Class="InstantDelivery.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InstantDelivery.Views"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        x:Name="InstantDelivery" Height="800" Width="1000" WindowState="Maximized" MinWidth="1024" MinHeight="680"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        GlowBrush="{DynamicResource AccentColorBrush}">
<controls:MetroWindow.IconTemplate>
    <DataTemplate>
        <Grid Width="{TemplateBinding Width}"
                 Height="{TemplateBinding Height}"
                 Margin="4"
                 Background="Transparent"
                 RenderOptions.EdgeMode="Aliased"
                 RenderOptions.BitmapScalingMode="HighQuality">
                 <Rectangle>
                     <Rectangle.Fill>
                         <VisualBrush Visual="{DynamicResource appbar_box}" />
                     </Rectangle.Fill>
                 </Rectangle>
        </Grid>
    </DataTemplate>
</controls:MetroWindow.IconTemplate>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Icons.xaml" />
                <ResourceDictionary Source="/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Style="{StaticResource LeftPanel}">
            <Expander Style="{StaticResource ExpanderLeftMenu}" Header="Kadry" Name="Kadry" IsExpanded="True">
                <StackPanel>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="Employees">Ogólne</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="PackagesEmployes">Obsługiwane przesyłki</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="EmployeesVehicles">Używane pojazdy</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="IdentifyEmployees">Dane identyfikacyjne</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="ContactEmployees">Dane kontaktowe</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="SalariesEmployees">Adresy zameldowania</Button>
                </StackPanel>
            </Expander>
            <Expander Style="{StaticResource ExpanderLeftMenu}" Header="Pojazdy" Name="Pojazdy" IsExpanded="True">
                <StackPanel>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="Vehicles">Ogólne</Button>
                    <!--<TextBlock Text="Akcje"/>
                    <Button x:Name="AddVehicles">Dodaj pojazd</Button>
                    <Button x:Name="RemoveVehicles">Usuń pojazd</Button>
                    <Button x:Name="UpdateVehicles">Edytuj pojazd</Button>-->
                </StackPanel>
            </Expander>
            <Expander Style="{StaticResource ExpanderLeftMenu}" Header="Przesyłki" Name="Packages" IsExpanded="True">
                <StackPanel>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="GeneralPackages">Ogólne</Button>
                    <!--<TextBlock Text="Akcje"/>
                    <Button x:Name="AddPackages">Dodaj przesyłkę</Button>
                    <Button x:Name="RemovePackages">Usuń przesyłkę</Button>
                    <Button x:Name="EditPackages">Edytuj przesyłkę</Button>
                    <Button x:Name="ConnectPackages">Przypisz przesyłkę</Button>-->
                </StackPanel>
            </Expander>
            <Expander Style="{StaticResource ExpanderLeftMenu}" Header="Zestawienia" Name="Zestawienia" IsExpanded="True">
                <StackPanel>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="DeliveredPackages">Dostarczone paczki</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="ManagedPackages">Efektywność pracowników</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="GeographicDetails">Zestawienie geograficzne</Button>
                    <Button Style="{StaticResource ButtonInExpanderLeftMenu}" x:Name="UsedVehicles">Użycie pojazdów</Button>
                </StackPanel>
            </Expander>
        </StackPanel>
        <controls:TransitioningContentControl Grid.Column="1" x:Name="ActiveItem" Transition="Right" Margin="10"/>
    </Grid>
</controls:MetroWindow>
