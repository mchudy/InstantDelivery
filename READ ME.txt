Przed pierwszym uruchomieniem aplikacji nale¿y najpierw skonfigurowaæ bazê danych.
Aby to zrobiæ nale¿y podj¹æ nastêpuj¹ce kroki:
	1. Otworzyæ okno "Package manager console".
	2. Przestawiæ 'default project' na "InstantDelivery.Core".
	3. W konsoli wpisac "update-database".
	   W przypadku problemów z poprawnym wykonaniem punktu 3 nale¿y wy³¹czyæ i w³¹czyæ projekt w Visual Studio, wybraæ Build -> Clean Solution i ponownie wpisaæ w odpowiedniej konsoli "update-database".
	4. Aby pomin¹æ krok wype³niania bazy danymi demonstracyjnymi pomin¹æ krok 5.
	5. W konsoli wpisaæ "update-database -verbose", w przypadku ostrze¿enia o mo¿liwoœci utracenia danych (co nie powinno nast¹piæ) nale¿y wpisaæ "update-database -verbose -force".
	6. Baza danych zosta³a skonfigurowana i zawiera dane demonstracyjne.

Tworz¹c aplikacjê korzystaliœmy z narzêdzia "ReSharper", m.in. ze specjalnego nugeta do xunit dla ReSharpera, odinstalowuj¹c przy tym domyœlny nuget do xunit dla Visual Studio. W Visual Studio bez zainstalowanego ReSharpera standardowe okno
pokazuj¹ce testy (Test -> Windows -> Text Explorer) nie bêdzie zawiera³o ¿adnych testów. Aby to umo¿liwiæ nale¿y zainstalowaæ domyœlny nuget do xunit. Aby to zrobiæ nale¿y:
	1. Otworzyæ okno "Package manager console".
	2. Przestawiæ 'default project' na "InstantDelivery.Tests".
	3. W konsoli wpisaæ "Install-Package xunit.runner.visualstudio".
	4. Zbudowaæ projekt "InstantDelivery.Tests".
	5. Odœwie¿yæ standardowe okno widoku testów.